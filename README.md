Drupal
======

Learnig GIT
  $form['address'] = array(
                      '#type' => 'textarea',
                      '#title' => t('Address'),
                      '#default_value' =>($row[address] == NULL) ? NULL : $row[address],
                      );
                      